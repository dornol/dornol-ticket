spring:
  flyway:
    enabled: true

  jpa:
    show-sql: true
    properties:
      hibernate.format_sql: true


server:
  servlet:
    context-path: /api/v1
logging:
  level:
    dev.dornol: debug

node-id: 0